# Add a .NET `.gitignore` file

The `dotnet` tool has a `new` template for adding a `.gitignore` file:

```
dotnet new gitignore
```

I usually add this to the repo root or my root source code directory.